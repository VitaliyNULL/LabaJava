CREATE TABLE IF NOT EXISTS advertisement (
                                             id BIGINT AUTO_INCREMENT PRIMARY KEY,
                                             street_name_uk VARCHAR(255),
    rooms_count INT,
    price INT,
    beautiful_url VARCHAR(255)
    );
